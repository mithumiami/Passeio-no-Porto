<p id="notice"></p>
<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-8 px-5 px-sm-0 mx-auto ">


        <h3 class="text-center my-4">本当に退会しますか？</h3>

        <div>
         <p>退会すると、会員登録情報や</p>
         <p>これまでの購入履歴が閲覧できなくなります。</p>
         <p>退会する場合は、「退会する」をクリックしてください。</p>
        </div>

        <div>
         <%= link_to "退会しない", user_path(current_user.id), class: 'btn btn-primary'%>
         <%= link_to "退会する",  goodbye_path(current_user.id), method: :patch, class: 'btn btn-danger'%>
        </div>
      </div>
    </div>
  </div>